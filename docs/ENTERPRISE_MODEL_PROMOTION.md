# 企业级预测模型晋级治理

`POST /api/enterprise/forecast/model-promotion` 为预测模型从验证环境进入生产环境提供可审计门禁。

系统校验回测 MAPE、系统性偏差、解释性、漂移、人工调整责任、冠军/挑战者评估、审批和回滚准备，输出 `PROMOTE`、`CANARY` 或 `BLOCKED`。

生产环境应记录数据快照、特征版本、模型摘要、阈值版本、审批人和回滚目标，并把上线后的准确率及业务影响接入持续监控。
